import React from 'react';
import ReactDOM from 'react-dom';
import './styles/LightTheme.css'; // Default to Light Mode
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
